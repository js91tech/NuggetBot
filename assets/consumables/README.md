# Consumable art

Drop optional GIFs here for `/use` consumable duel effects.

- `sakunas_finger.gif` — plays on Malevolent Shrine deflect procs in duels (overrides `SAKUNAS_FINGER_GIF_URL` in config when present).
